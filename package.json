{
  "name": "jw-video-transcript",
  "private": true,
  "scripts": {
    "build": "node scripts/build.cjs",
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "esbuild": "0.25.9",
    "jsdom": "26.1.0"
  }
}
